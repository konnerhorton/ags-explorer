import streamlit as st


def render_load_file_page():
    st.header("Load ags file")
    st.write(
        "Load the ags file using the `Home` page (accessed via the menu on the left) to view the data."
    )
